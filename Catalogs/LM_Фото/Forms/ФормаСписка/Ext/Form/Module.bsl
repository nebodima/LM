
&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	ОтобразитьФотоНаСервере();
КонецПроцедуры

Процедура ОтобразитьФотоНаСервере()
	Попытка
		Фото = ПоместитьВоВременноеХранилище(Элементы.Список.ТекущаяСтрока.Файл.Получить());
	Исключение
	КонецПопытки;
КонецПроцедуры

